# Facebook Groups Poster — Python dependencies
aiohttp>=3.9.0
anthropic>=0.40.0
playwright>=1.44.0

# fb_check_groups.py uses the synchronous websocket-client (import websocket)
websocket-client>=1.7.0

# fb_export_cookies.py uses websockets to talk to Chrome CDP
websockets>=12.0
